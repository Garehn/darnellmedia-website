# Copy this file to `.env` (no extension) and fill in real values.
# .env is gitignored — your real keys never get committed.

# OpenAI — used by scripts/generate_images.py for hero, OG, and inline images.
# Get a key at: https://platform.openai.com/api-keys
# Add ~$15 credit to the account for a 100-image run.
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxx
